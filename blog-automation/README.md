# Blog Automation

GitHub → Notion blog automation via Cloudflare Worker. Full setup, Notion hubs, and deploy: **[docs/blog-automation.md](../docs/blog-automation.md)**.

```bash
cd blog-automation && npm install && npx wrangler deploy
```
